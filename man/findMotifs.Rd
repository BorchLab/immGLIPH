% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/findMotifs.R
\name{findMotifs}
\alias{findMotifs}
\title{Find continuous and discontinuous sequence motifs}
\usage{
findMotifs(seqs, q = 2:4, kmer_mindepth = NULL, discontinuous = FALSE)
}
\arguments{
\item{seqs}{A character vector of amino acid sequences in which motifs
will be identified and counted.}

\item{q}{A numeric vector of motif lengths to search for.
\strong{Default:} \code{2:4}.}

\item{kmer_mindepth}{The minimum number of times a k-mer must be observed
in \code{seqs} for it to be included in the output.
\strong{Default:} \code{NULL} (no filtering).}

\item{discontinuous}{Whether to include discontinuous (gapped) motifs in
the search. \strong{Default:} \code{FALSE}.}
}
\value{
A \code{data.frame} with two columns: \code{motif} (the k-mer
string) and \code{V1} (the observed frequency).
}
\description{
Searches a character vector of amino acid sequences for k-mer motifs and
returns their frequencies. Both continuous and discontinuous (gapped) motifs
are supported. When \pkg{immApex} (>= 2.0.0) is installed, the
C++-accelerated \code{immApex::calculateMotif()} backend is used
automatically for improved performance; otherwise the function falls back
to a pure-R implementation based on \code{\link[stringdist]{qgrams}}.
}
\examples{
utils::data("gliph_input_data")
sample_seqs <- as.character(gliph_input_data$CDR3b)
res <- findMotifs(seqs = sample_seqs)

}
