% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotNetwork.R
\name{plotNetwork}
\alias{plotNetwork}
\title{Visualize TCR convergence group network}
\usage{
plotNetwork(
  clustering_output = NULL,
  result_folder = "",
  show_additional_columns = NULL,
  color_info = "total.score",
  color_palette = viridis::viridis,
  local_edge_color = "orange",
  global_edge_color = "#68bceb",
  size_info = NULL,
  absolute_size = FALSE,
  cluster_min_size = 3,
  n_cores = 1
)
}
\arguments{
\item{clustering_output}{Output list returned by \code{\link{runGLIPH}}.
\strong{Default:} \code{NULL}}

\item{result_folder}{Path to the folder containing saved GLIPH output
files. When a non-empty path is supplied the results are loaded from disk
and \code{clustering_output} is ignored. \strong{Default:} \code{""}}

\item{show_additional_columns}{Character vector of extra column names whose
values should be displayed in the node tooltips. Column names from the
original \code{cdr3_sequences} data frame and from
\code{clustering_output$cluster_properties} are accepted.
\strong{Default:} \code{NULL}}

\item{color_info}{Column name used to colour the nodes. Accepts any column
from the input \code{cdr3_sequences} or
\code{clustering_output$cluster_properties}. Set to \code{"none"} to
colour all nodes grey, or \code{"color"} to use pre-assigned colour
values stored in that column. For numeric columns the viridis palette is
applied automatically (purple = low, yellow = high).
\strong{Default:} \code{"total.score"}}

\item{color_palette}{A function that accepts a single integer \code{n} and
returns \code{n} colour values. \strong{Default:} \code{viridis::viridis}}

\item{local_edge_color}{Colour applied to edges representing local
similarities. \strong{Default:} \code{"orange"}}

\item{global_edge_color}{Colour applied to edges representing global
similarities. \strong{Default:} \code{"#68bceb"}}

\item{size_info}{Column name whose numeric values determine node sizes.
Accepts columns from \code{cdr3_sequences} or
\code{clustering_output$cluster_properties}. \strong{Default:} \code{NULL}}

\item{absolute_size}{If \code{TRUE} the raw values from the
\code{size_info} column are used as node sizes; otherwise the values are
linearly scaled to the range 12--20. \strong{Default:} \code{FALSE}}

\item{cluster_min_size}{Minimum number of members a cluster must contain to
be included in the plot. \strong{Default:} \code{3}}

\item{n_cores}{Number of cores for parallel processing. When \code{NULL}
the number of available cores minus two is used. \strong{Default:} \code{1}}
}
\value{
A \code{visNetwork} object containing the interactive network graph.
}
\description{
Uses the visNetwork package to build an interactive network graph from the
clustering results produced by \code{\link{runGLIPH}}. Nodes represent
individual CDR3b sequences and edges encode local or global sequence
similarities. The resulting visualization is fully interactive: scroll to
zoom, hover over a node for details, and click a node to highlight its
direct neighbors.
}
\examples{
utils::data("gliph_input_data")
ref_df <- gliph_input_data[, c("CDR3b", "TRBV")]
res <- runGLIPH(cdr3_sequences = gliph_input_data[seq_len(200),],
                method = "gliph1",
                refdb_beta = ref_df,
                sim_depth = 100,
                n_cores = 1)

plotNetwork(clustering_output = res,
            n_cores = 1)

}
